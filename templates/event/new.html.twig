{% extends 'base.html.twig' %}

{% block title %}New Event{% endblock %}

{% block body %}
    <h1>Create new Event</h1>
    <div>
    {{ include('event/_form.html.twig') }}
    
    <a class="btn btn-secondary"href="{{ path('app_event_index') }}">back to list</a>
    </div>
{% endblock %}
